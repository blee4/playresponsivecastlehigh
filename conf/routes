# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /StudentsParents            controllers.Application.studentsParents()
GET     /FacultyStaff               controllers.Application.facultyStaff()
GET     /AlumniFriends              controllers.Application.alumniFriends()
GET     /ContactUs                  controllers.Application.contactUs()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)